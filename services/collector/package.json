{
  "name": "@phoenix/collector",
  "version": "1.0.0",
  "description": "Phoenix core OpenTelemetry collector service",
  "private": true,
  "scripts": {
    "build": "echo 'Collector uses OTEL binary'",
    "build:docker": "docker build -f Dockerfile -t phoenix-collector:latest .",
    "test": "echo 'No tests for collector config'",
    "lint": "yamllint config/*.yaml",
    "dev": "docker-compose -f ../../infrastructure/docker/compose/dev.yaml up collector"
  },
  "dependencies": {
    "@phoenix/contracts": "workspace:*"
  }
}